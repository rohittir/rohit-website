{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "build/app.js",
  "scripts": {
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf build && npm run tsc && node build/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.1",
    "@types/node": "^12.0.1",
    "express": "^4.16.4",
    "typescript": "^3.4.5"
  }
}
